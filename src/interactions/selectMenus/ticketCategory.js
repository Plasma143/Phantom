// src/interactions/selectMenus/ticketCategory.js
//
// Handles the ticket category dropdown — fires when a user picks a ticket type
// from the select menu on the ticket panel. Shows a modal for description after.

import { ticketCategorySelectMenu } from '../../handlers/ticketButtons.js';

export default ticketCategorySelectMenu;
